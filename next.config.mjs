/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['example.com', 'images.unsplash.com','localhost'], // Add all domains you use for product images
  },
  
};

export default nextConfig;
